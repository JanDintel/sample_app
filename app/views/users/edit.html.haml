-provide(:title, "Settings")

%h1 Update your profile

.row
  .center
    = simple_form_for @user do |f|
      = render 'shared/error_messages'
      = f.input :name
      = f.input :email
      = f.input :password
      = f.input :password_confirmation, label: "Confirmation", required: true
      = f.button :submit, "Change my account", class: "btn btn-large btn-primary"
    = gravatar_for @user
    %a{href: "http://gravatar.com/emails"} Change avatar?
